// Preload script — runs in renderer context before page loads
// Kept minimal for security (contextIsolation: true)
window.addEventListener('DOMContentLoaded', () => {
  // Nothing needed — app loads the live Vercel site
});
